"""RQ job function — the enqueued callable.

Phase 3 fills this in with ``run_task(qc)``, which executes the circuit and
writes a terminal completed/error state without re-raising handled failures
(terminal-state guarantee, REFERENCE §7.4).
"""

# TODO(Phase 3): implement run_task(qc) enqueued callable.
