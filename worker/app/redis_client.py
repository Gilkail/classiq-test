"""Redis connectivity for the worker service.

Phase 1 fills this in with a singleton ``redis.Redis`` built from REDIS_URL,
used both by the RQ worker bootstrap and the terminal-state writer.
"""

# TODO(Phase 1): implement get_redis() singleton.
