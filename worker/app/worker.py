"""RQ worker entrypoint.

Phase 3 fills this in with the bootstrap that builds the Redis connection and
starts ``rq.Worker([queue]).work()`` with structured logging configured.
Invoked as ``python -m app.worker`` from the worker container.
"""

# TODO(Phase 3): implement worker bootstrap / entrypoint.
