# Worker service dependencies (heavy Qiskit/Aer stack lives only here).
# Unpinned for Phase 0 scaffolding; exact versions are pinned in Phase 4 once
# images are first built and compatibility is confirmed
# (Qiskit >= 1.1 / qiskit-aer 0.17.x / qiskit-qasm3-import >= 0.6).
rq
redis
qiskit
qiskit-aer
qiskit-qasm3-import
