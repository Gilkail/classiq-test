"""Task state accessor for the API service (``task:{id}`` HASH).

Phase 2 fills this in with ``create_pending()``, ``get()``, and ``rollback()``
helpers that read/write the canonical task-state hash (REFERENCE §5.1).
"""

# TODO(Phase 2): implement create_pending(), get(), rollback().
