"""FastAPI application for the Quantum Circuit Execution Engine API.

Phase 2 fills this in with the public HTTP contract (REFERENCE §6):
``POST /tasks``, ``GET /tasks/{id}``, ``GET /health``, plus the validation
exception handlers that shape Pydantic errors into the documented 400 body.
"""

# TODO(Phase 2): implement FastAPI app, routes, and exception handlers.
