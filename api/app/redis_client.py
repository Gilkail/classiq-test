"""Redis connectivity for the API service.

Phase 1 fills this in with a singleton ``redis.Redis`` built from REDIS_URL and
a helper returning an RQ ``Queue`` for enqueuing jobs.
"""

# TODO(Phase 1): implement get_redis() singleton + get_queue() RQ factory.
